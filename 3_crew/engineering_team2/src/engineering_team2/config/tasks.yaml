
design_task:
  description: >
    Take the high level requirements described here and prepare a detailed design for the engineer;
    everything should be in 1 python module, but outline the classes and methods in the module.
    Here are the requirements: {requirements}
    IMPORTANT: Only output the design in markdown format, laying out in detail the classes and functions in the module, describing the functionality.
  expected_output: >
    A detailed design for the engineer, identifying the classes and functions in the module.
  agent: engineering_lead
  output_file: output/{module_name}_design.md

code_task:
  description: >
    Write a python module that implements the design described by the engineering lead, in order to achieve the requirements.
    Here are the requirements: {requirements}
  expected_output: >
    A python module that implements the design and achieves the requirements.
    IMPORTANT: Output ONLY the raw Python code without any markdown formatting, code block delimiters, or backticks.
    The output should be valid Python code that can be directly saved to a file and executed.
  agent: backend_engineer
  context:
    - design_task
  output_file: output/{module_name}

frontend_task:
  description: >
    Write a gradio UI in a module app.py that demonstrates the given backend class in {module_name}.
    Assume there is only 1 user, and keep the UI very simple indeed - just a prototype or demo.
    Here are the requirements: {requirements}
  expected_output: >
    A gradio UI in module app.py that demonstrates the given backend class.
    The file should be ready so that it can be run as-is, in the same directory as the backend module, and it should import the backend class from {module_name}.
    IMPORTANT: Output ONLY the raw Python code without any markdown formatting, code block delimiters, or backticks.
    The output should be valid Python code that can be directly saved to a file and executed.
  agent: frontend_engineer
  context:
    - code_task
  output_file: output/app.py

test_task:
  description: >
    Write unit tests for the given backend module {module_name} and create a test_{module_name} in the same directory as the backend module.
  expected_output: >
    A test_{module_name} module that tests the given backend module.
    IMPORTANT: Output ONLY the raw Python code without any markdown formatting, code block delimiters, or backticks.
    The output should be valid Python code that can be directly saved to a file and executed.
  agent: test_engineer
  context:
    - code_task
  output_file: output/test_{module_name}


note_taking_task:
  description: >
    Take the user's requirements and extract key information:
    - Key objectives
    - Constraints
    - Open questions
    - Important assumptions
    Present the extracted information in a clear, structured markdown format.
    Here are the requirements: {requirements}
  expected_output: >
    A structured markdown with sections: Objectives, Constraints, Open Questions, Assumptions.
  agent: secretary_note_taker
  output_file: output/note.md

refined_note_taking_task:
  description: >
    Based on the user's feedback, refine and update the notes. 
    The user's feedback is: {user_feedback}
    Carefully incorporate the feedback into the notes.
    Update the structured notes with the following sections: Objectives, Constraints, Open Questions, Assumptions.
  expected_output: >
    Refined structured markdown with sections: Objectives, Constraints, Open Questions, Assumptions.
    The notes should incorporate the user's feedback.
  agent: secretary_note_taker
  context:
    - note_taking_task
  output_file: output/refined_note.md

executer_task:
  description: >
    Using the notes from the secretary, create a Gradio UI that implements the requirements.
    The notes contain the structured requirements with Objectives, Constraints, Open Questions, and Assumptions.
    Here are the notes: {refined_notes}
    Create a simple, functional Gradio interface based on these requirements.
  expected_output: >
    A gradio UI in module app.py that implements the requirements from the notes.
    IMPORTANT: Output ONLY the raw Python code without any markdown formatting, code block delimiters, or backticks.
    The output should be valid Python code that can be directly saved to a file and executed.
  agent: frontend_engineer
  output_file: output/app.py